#include <stdio.h>
int main() {
    int n,i,product;
    printf("Enter the value of n: ");
    scanf("%d",&n);
    printf("Multiplication table of %d:\n", n);
    for (i =1; i<=10;i++) {
        product = n*i;
        printf("%d x %d = %d\n",n,i,product);
    }
    return 0;
}
